{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn onehub.wsgi --log-file -",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
